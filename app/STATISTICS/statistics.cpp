#ifndef MEASUREMENT_STATS_

#pragma once
#include <stdlib.h>
#include <cstdint>

void sort_array(uint8_t* arr, uint8_t size);
float calculate_median(uint8_t* arr, uint8_t length);
float calculate_avg(uint8_t* arr,uint8_t lenght);

#endif